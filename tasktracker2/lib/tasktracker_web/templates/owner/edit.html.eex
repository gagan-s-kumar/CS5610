<h2>Edit Owner</h2>

<%= render "form.html", Map.put(assigns, :action, owner_path(@conn, :update, @owner)) %>

<span><%= link "Go to Task", to: task_path(@conn, :index), class: "btn btn-primary" %></span>
